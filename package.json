{
  "name": "test-workspace-root",
  "version": "1.0.0",
  "description": "Test NPM workspace configuration for repo jacking testing",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/frontend", 
    "apps/backend",
    "shared/utils",
    "tools/build-scripts"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces", 
    "dev": "npm run dev --workspace=apps/frontend",
    "url": "https://github.com/nonexistent-org123123123/fake-api-client.git"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0",
    "eslint": "^8.0.0"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
